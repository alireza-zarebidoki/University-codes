# کاراگاه مرتب (Sorted Detective)

## 📝 صورت سوال

برنامه‌ای بنویسید که یک آرایه‌ی مرتب صعودی از اعداد صحیح و یک عدد هدف را دریافت کند. اگر عدد هدف در آرایه وجود داشت، اندیس آن را چاپ کند و در غیر این صورت، عدد `-1` را خروجی دهد.

### ⚙️ محدودیت‌ها

  * **زمان:** ۱ ثانیه
  * **حافظه:** ۲۵۶ مگابایت
  * **محدوده اعداد:** $0 \le n \le 10^7$ و $-10^9 \le a_i, target \le 10^9$

-----

## 📥 ورودی

ورودی شامل سه خط است:

1.  خط اول: عدد صحیح $n$ (تعداد اعضای آرایه).
2.  خط دوم: $n$ عدد صحیح مرتب‌شده $[a_1, a_2, \dots, a_n]$ به صورت صعودی.
3.  خط سوم: عدد صحیح $target$ (عددی که باید جست‌وجو شود).

## 📤 خروجی

خروجی باید تنها یک عدد باشد: اندیس عدد هدف در آرایه (در صورت وجود) و در غیر این صورت `-1`.
(توجه: اندیس‌گذاری از ۰ شروع می‌شود).

-----

### 💡 نمونه‌ها

**ورودی نمونه ۱:**

```text
5
2 4 5 6 10
6
```

**خروجی نمونه ۱:**

```text
3
```

**ورودی نمونه ۲:**

```text
6
-5 -2 0 3 7 10
0
```

**خروجی نمونه ۲:**

```text
2
```

**ورودی نمونه ۳:**

```text
4
1 3 5 7
4
```

**خروجی نمونه ۳:**

```text
-1
```
